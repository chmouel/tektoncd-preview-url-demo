---
apiVersion: v1
kind: List
metadata: {}
items:
- apiVersion: extensions/v1beta1
  kind: Deployment
  metadata:
    name: openshift-pipelines-demo-%SHA%
    labels:
      deployment: openshift-pipelines-demo-%SHA%
      generated: openshift-pipelines-sha
  spec:
    selector:
      matchLabels:
        deployment: openshift-pipelines-demo-%SHA%
    template:
      metadata:
        labels:
          deployment: openshift-pipelines-demo-%SHA%
      spec:
        containers:
        - image: image-registry.openshift-image-registry.svc.cluster.local:5000/tekton-demo/demo:%FULLSHA%
          name: web
- apiVersion: v1
  kind: Service
  metadata:
    labels:
      name: openshift-pipelines-demo-%SHA%
    name: openshift-pipelines-demo-%SHA%
  spec:
    ports:
    - name: 8080-tcp
      port: 8080
      protocol: TCP
      targetPort: 8080
    selector:
      deployment: openshift-pipelines-demo-%SHA%
